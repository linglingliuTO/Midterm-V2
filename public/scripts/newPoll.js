// function to  remove an option if the user clicks on the delete button


const deleteOption = () => {
  const min = 2;
  $("#deletePoll").click(function(e) {
    e.preventDefault();
    // const optParent = document.getElementById('options-parent')
    // const optChild = optField.children;
    // optChild[countOptions() - 1].remove()
    // if(countOptions() > min) {
    //   $('#deletePoll').show();
    // } else {
    //   $('#deletePoll').hide();
    // }
    console.log("hello")
  });
}
